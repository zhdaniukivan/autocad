📋 Основные вкладки
Программа имеет две основные вкладки:
1. 📊 «Группа (несколько блоков)» — основная вкладка
Как пользоваться:

Выделите нужные динамические блоки в AutoCAD.
Нажмите кнопку «🔄 Обновить выделение из AutoCAD».
В таблице появятся все выделенные блоки с их параметрами.

Что показывается в таблице:

Тип — Нижний / Верхний / ?
Ширина и Высота
Менять ширину (ChangeWidth = 1)
Менять высоту (ChangeDepth = 1)

Групповые операции:
Изменение ширины:

all_ширина (нижние блоки) — задаёте желаемую общую длину всех нижних блоков.
all_ширина (верхние блоки) — то же для верхних.
Есть галочка «Синхронно (низ+верх)» — очень удобно, когда нужно одинаковую длину.

Программа автоматически распределит прибавку ширины только между блоками, у которых ChangeWidth = 1.
Изменение высоты:

Поле all_длинна (высота для ChangeDepth=1).
Применяется только к блокам с флагом ChangeDepth = 1.
Высота увеличивается только если новая больше текущей.


2. 🔧 «Один блок (свойства)»
Как пользоваться:

Выделите ровно один блок в AutoCAD.
Нажмите «🎯 Выбрать блок в AutoCAD».
Появится список всех динамических свойств блока.


По умолчанию показываются только основные параметры (Width, Height, isTop, isBottom, ChangeWidth, ChangeDepth).
Кнопка «📋 Показать все параметры» — раскрывает полный список.

Как редактировать:

Вводите новое значение → жмёте кнопку «Применить» напротив параметра.
Или редактируйте всё и нажмите большую кнопку «✅ Применить все изменения» внизу.


🔄 Другие кнопки





















КнопкаДействие🔄 Обновить выделениеЗагружает текущее выделение из AutoCAD❌ СбросОчищает данные текущей вкладки💾 Сохранить чертёжСохраняет DWG-файл

Важные особенности и советы

Работает только с динамическими блоками (AcDbBlockReference + IsDynamicBlock = True).
Программа увеличивает размеры, но не уменьшает (защита от ошибок).
При изменении ширины увеличение распределяется равномерно только между блоками с ChangeWidth=1.
Если нужно изменить высоту — блок должен иметь свойство ChangeDepth=1.
После изменений программа автоматически делает Regen (перерисовку).


Структура файлов

main.py — запуск программы
gui.py — весь интерфейс и логика
autocad_handler.py — работа с AutoCAD через COM (win32com)
debug.log — файл лога (полезен при ошибках)


Возможные проблемы

Не подключается к AutoCAD → убедитесь, что AutoCAD запущен до запуска программы.
Не видит блоки → блоки должны быть динамическими и выделены.
Ошибки при применении → смотрите файл debug.log в папке с программой.